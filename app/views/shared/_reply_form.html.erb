<%= form_for(@reply) do |f| %>
    <%= render 'shared/error_messages', :object => f.object %>
    <div class="field">
      <%= f.hidden_field :content, :placeholder => "Reply to microposts..." %>
    </div>
    <%= f.submit "Reply", :class => "btn btn-small btn-primary" %>
<% end %>
