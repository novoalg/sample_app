<% unless current_user?(@user) %>
  <div id="follow_form">
  <% if signed_in? %>
  <% if current_user.following?(@user) %>
    <%= render 'unfollow' %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  <% else %>
    <%= render 'follow' %>
  <% end %>
  </div>
<% end %>
